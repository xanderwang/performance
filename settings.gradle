println("-" * 100)
def composingMode = true
rootProject.name = 'performace'
if (composingMode) {
  includeBuild("plugin")
} else {
  include ':plugin'
}
include ':lib-epic'
include ':app'
include ':tool'
include ':tool-noop'
